#include "utils.h"

int main(int argc, char** argv){

    // game* g = create_game(0, 1);

    // g->board[3] = X;

    // printf("%s\n", board_to_string(g->board));

    // char gm[MAX_MESSAGE_LEN];
    // sprintf(gm, "Ziutek:%s:%c",board_to_string(g->board), sign_to_char(X));

    // game_message* msg = parse_game_message(gm);

    // printf("Result:\nname: %s\nboard: %s\nsign: %c\n",msg->opponent, msg->board, msg->sign);

    // GAME_STATUS gs = DRAW;
    // printf("%s\n", game_status_to_string(gs));

    char c;
    scanf("%c", &c);
    printf("%c\n", c);
    
    return 0;
}